import { Meta } from '@storybook/addon-docs/blocks';

import { Annotations } from '../blocks.tsx';

<Meta title="Docs/Annotations" />

# Annotations

The read-only Docs block below renders annotations for the `Demo` story. Leave
comments via the "Annotate" toolbar toggle on the canvas, then reload this page.

<Annotations storyId="demo" />
