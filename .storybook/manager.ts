import { addons } from 'storybook/manager-api';

// The annotations addon registers itself via the built manager entry
// (see .storybook/local-preset.ts). Nothing else to configure here.
addons.setConfig({});
